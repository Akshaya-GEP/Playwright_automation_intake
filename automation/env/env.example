## Required
# Base application URL (where the login page lives)
BASE_URL=https://your-app.example.com

# Login credentials
USER_ID=your_user_id
PASSWORD=your_password

## Required after login
# The Qube Mesh URL you want to land on after logging in.
# Can be absolute (recommended).
# NOTE: In some environments Qube Mesh is hash-routed; include "#/dashboard-landing-page".
# IMPORTANT: Quote the value because some dotenv parsers treat `#` as a comment delimiter.
QUBE_MESH_URL="https://quantumdev.gep.com/DeveloperDomain/v3/#/dashboard-landing-page"

## Test inputs
# Used for the "user query" field after selecting an agent.
USER_QUERY=Hello from Playwright

# Agent 2 specific starting query (contract amendment example)
USER_QUERY2=I want to amend the contract CDR0028258.0

# Agent 2: Amendment reason selection (optional)
# Recommended: set to visible option text (stable even if the list order changes)
# Example: REASON_AMEND=Addition of new locations or regions
# Alternative: set to a 1-based index (e.g. "2" selects the 2nd option)
REASON_AMEND=

# Agent 3 specific starting query (contract termination example)
USER_QUERY3=I want to terminate contract CDR0027626

# Agent 3/3_1: termination flow controls (optional)
# TERMINATION_STATUS can be:
# - future      -> "Terminate for a Future Date" (runs date picker step)
# - immediate   -> "Terminate Immediately" (skips date step)
TERMINATION_STATUS=future

# Agent 3/3_1: termination reason selection (optional)
# Recommended: set to visible option text (e.g. "Termination for cause")
REASON_TERMINATE=

## Agent 1 specific inputs
# Select one of the offboard reasons (label text) in the UI
REASON_OFFBOARD=

# Optional: supplier to pick from the grid (label text). If empty, first supplier is selected.
SUPPLIER_NAME=

## Agents
# Option A (recommended): set each one explicitly
AGENT_1=Supplier offboarding assistant
AGENT_2=Contract Amendment Assistant
AGENT_3=Contract Termination Assistant
AGENT_4=Agent 4
AGENT_5=Agent 5

# Option B: comma-separated list (must be 5 values)
# AGENTS=Agent 1,Agent 2,Agent 3,Agent 4,Agent 5

## Agent 4 specific inputs
# Extension date - can be provided as full date or separate components
# Option 1: Full date string (YYYY-MM-DD or YYYY/MM/DD)
EXTENSION_DATE_4=2028-03-09
# Option 2: Separate components
# EXTENSION_YEAR_4=2028
# EXTENSION_MONTH_4=03
# EXTENSION_DAY_4=09

# Agent 4: Contract modification choice (optional)
# Options: "Keep unchanged" or "Propose modifications"
MODIFICATIONS=Keep unchanged

# Agent 4: Update option for continuing with current supplier (optional)
# Example: "cost efficiency"
UPDATE_OPTION=cost efficiency

# Agent 4: Currency for contract budget (optional)
# Example: "EUR", "USD", "GBP"
CURRENCY=EUR

# Agent 4: Estimated cost for contract (optional)
# Example: "1000", "5000"
ESTIMATED_COST=1000

# Agent 4: Budget approval status (optional)
# Options: "Draft - Not Yet Submitted", "Submitted - Awaiting Approval", "Approved", "Approved with Conditions", "Budget Not Approved"
APPROVAL=Approved

## Agent 5 specific inputs
# Agent 5: Initial query for supplier profile update
USER_QUERY_5=I want to update supplier name Microsoft

# Agent 5: Supplier name and code to select from table (optional)
# If not provided, will select first supplier in table
SUPPLIER_NAME_5=Microsoft
SUPPLIER_CODE_5=GEP-000010742

# Agent 5: Update type to select from dropdown (optional)
# Example: "Update" or "Block/ Unblock Supplier"
UPDATE_TYPE_5=Block/ Unblock Supplier

# Agent 5: Reason/action description for the update (optional)
REASON_ACTION_5=Block supplier because he was rude

# Agent 5: File path for document upload (optional)
# Example: "./test-files/Employee Directory.xlsx"
UPLOAD_FILE_5=


